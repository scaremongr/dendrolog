# cmd некорректно разбирает батники с LF-концами строк (режет команды
# посередине при не-ASCII тексте), поэтому фиксируем CRLF независимо от
# настроек core.autocrlf на конкретной машине.
*.bat text eol=crlf
*.cmd text eol=crlf
